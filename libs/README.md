# Shared Libraries

Shared code is split by language:

- `go/`: reusable packages for multiple Go services
- `ts/`: shared frontend/client utilities and types
- `python/`: reusable research and analysis modules

Canonical contract adoption notes live in each language directory README so future services can share one validation path.
