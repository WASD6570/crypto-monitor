# Python Offline Parity

- Python contract helpers are optional and offline-only.
- Reuse `tests/fixtures/` and `tests/replay/` directly for parity or research validation.
- Do not introduce Python as a required runtime dependency for live services or the SPA.
