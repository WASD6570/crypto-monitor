{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "schemas/json/events/order-book-top.v1.schema.json",
  "title": "Order Book Top Event v1",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "schemaVersion",
    "eventType",
    "symbol",
    "sourceSymbol",
    "quoteCurrency",
    "venue",
    "marketType",
    "exchangeTs",
    "recvTs",
    "timestampStatus",
    "sourceRecordId"
  ],
  "properties": {
    "schemaVersion": {
      "const": "v1"
    },
    "eventType": {
      "const": "order-book-top"
    },
    "bookAction": {
      "type": "string",
      "enum": ["snapshot", "delta", "top-of-book"]
    },
    "symbol": {
      "type": "string",
      "enum": ["BTC-USD", "ETH-USD"]
    },
    "sourceSymbol": {
      "type": "string",
      "minLength": 1
    },
    "quoteCurrency": {
      "type": "string",
      "enum": ["USD", "USDT", "USDC"]
    },
    "venue": {
      "type": "string",
      "enum": ["BINANCE", "BYBIT", "COINBASE", "KRAKEN"]
    },
    "marketType": {
      "type": "string",
      "enum": ["spot", "perpetual"]
    },
    "exchangeTs": {
      "anyOf": [
        {
          "type": "string",
          "format": "date-time"
        },
        {
          "type": "string",
          "const": ""
        }
      ]
    },
    "recvTs": {
      "type": "string",
      "format": "date-time"
    },
    "timestampStatus": {
      "type": "string",
      "enum": ["normal", "degraded"]
    },
    "sourceRecordId": {
      "type": "string",
      "minLength": 1
    }
  }
}
