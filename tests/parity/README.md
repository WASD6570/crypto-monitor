# Parity Tests

- Use this directory for deterministic cross-language parity checks only.
- Reuse canonical fixtures and replay seeds from `tests/fixtures/` and `tests/replay/`.
- Limit parity checks to shared contract interpretation or other logic that must agree across languages.
